module.exports = {
    presets: [
      '@babel/preset-env', // Transpile modern JavaScript to a compatible version
      '@babel/preset-react', // Transpile React JSX code
    ],
    plugins: [
      // Add any additional Babel plugins if needed
    ],
  };
  